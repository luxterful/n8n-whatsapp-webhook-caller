services:
  bot:
    build: .
    ports:
      - "3000:3000"
    environment:
      - WEBHOOK_URL=${WEBHOOK_URL}
    volumes:
      - auth_data:/app/auth_info
    restart: unless-stopped

volumes:
  auth_data:
